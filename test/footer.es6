import { ArticleFooterContainer } from '../footer';

import chai from 'chai';
const should = chai.should();
describe('footer', () => {

  it('has ArticleFooterContainer exposed', () => {
    should.exist(ArticleFooterContainer);
  });

});
