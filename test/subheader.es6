import { ArticleSubheaderContainer } from '../subheader';

import chai from 'chai';
const should = chai.should();
describe('subheader', () => {

  it('has ArticleSubheaderContainer exposed', () => {
    should.exist(ArticleSubheaderContainer);
  });

});
